<!DOCTYPE html>
<html lang="zh-HK">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>婚禮規劃 Web App</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Vue 3 -->
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>

  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            pinky: '#F6C1D1',
            minty: '#C9EDE0',
            skysoft: '#CFE8F7',
            lavender: '#E6D9F7'
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gradient-to-br from-pinky via-lavender to-minty min-h-screen flex justify-center">

<div id="app" class="w-full max-w-md bg-white/80 backdrop-blur rounded-3xl shadow-xl m-4 overflow-hidden">

  <!-- Header -->
  <div class="p-4 text-center font-semibold text-lg">💍 婚禮規劃</div>

  <!-- Tabs -->
  <div class="grid grid-cols-4 text-xs text-center border-t">
    <button v-for="t in tabs" :key="t.key"
      @click="currentTab = t.key"
      :class="['p-2', currentTab===t.key ? 'bg-pinky/40 font-semibold' : '']">
      {{ t.label }}
    </button>
  </div>

  <!-- Content -->
  <div class="p-4 space-y-4">

    <!-- 行程表 -->
    <div v-if="currentTab==='schedule'" class="space-y-3">
      <h2 class="font-semibold">⛪ 婚禮當天行程</h2>
      <div v-for="item in schedule" class="bg-white rounded-xl p-3 shadow">
        <div class="text-sm font-medium">{{ item.time }}</div>
        <div class="text-gray-600 text-xs">{{ item.title }}</div>
      </div>
    </div>

    <!-- 收禮記錄 -->
    <div v-if="currentTab==='gifts'" class="space-y-3">
      <div class="flex gap-2">
        <button class="flex-1 rounded-xl p-2"
          :class="giftType==='gold'?'bg-pinky':'bg-gray-100'"
          @click="giftType='gold'">🪙 金器</button>
        <button class="flex-1 rounded-xl p-2"
          :class="giftType==='red'?'bg-pinky':'bg-gray-100'"
          @click="giftType='red'">🧧 利是/禮物</button>
      </div>
      <div v-for="g in gifts[giftType]" class="bg-white rounded-xl p-3 shadow text-sm">
        {{ g }}
      </div>
    </div>

    <!-- 資訊 -->
    <div v-if="currentTab==='info'" class="space-y-3">
      <div class="flex gap-1 text-xs">
        <button v-for="i in infoTabs" :key="i"
          @click="infoTab=i"
          :class="['flex-1 p-2 rounded-xl', infoTab===i?'bg-minty':'bg-gray-100']">
          {{ i }}
        </button>
      </div>
      <ul class="bg-white rounded-xl p-3 shadow text-sm space-y-1">
        <li v-for="x in info[infoTab]">• {{ x }}</li>
      </ul>
    </div>

    <!-- 聯絡 -->
    <div v-if="currentTab==='contacts'" class="space-y-3">
      <div v-for="(list, group) in contacts" class="bg-white rounded-xl p-3 shadow">
        <div class="font-medium text-sm mb-1">{{ group }}</div>
        <div v-for="c in list" class="text-xs text-gray-600">
          {{ c.name }}（{{ c.role }}）- {{ c.phone }}
        </div>
      </div>
    </div>

  </div>
</div>

<script>
const { createApp } = Vue
createApp({
  data(){
    return {
      currentTab: 'schedule',
      giftType: 'gold',
      infoTab: '物資List',
      tabs: [
        { key:'schedule', label:'⛪ 行程' },
        { key:'gifts', label:'🧧 收禮' },
        { key:'info', label:'📝 資訊' },
        { key:'contacts', label:'📞 聯絡' }
      ],
      schedule: [
        { time:'09:00', title:'新娘化妝' },
        { time:'11:00', title:'接新娘' },
        { time:'15:00', title:'教堂儀式' }
      ],
      gifts: {
        gold: ['龍鳳鈪一對', '金頸鏈'],
        red: ['張三 $2000', '李四 香檳一支']
      },
      infoTabs: ['物資List','影相次序','晚宴座位表'],
      info: {
        '物資List': ['戒指','誓詞卡','花球'],
        '影相次序': ['新郎新娘','雙方父母','大合照'],
        '晚宴座位表': ['主家席','親友席','朋友席']
      },
      contacts: {
        '新郎': [{name:'阿明', role:'新郎', phone:'91234567'}],
        '新娘': [{name:'阿欣', role:'新娘', phone:'98765432'}],
        '教堂': [{name:'陳牧師', role:'牧師', phone:'90001111'}],
        '晚宴': [{name:'酒店經理', role:'Coordinator', phone:'93334444'}],
        '其他': []
      }
    }
  }
}).mount('#app')
</script>

</body>
</html>
